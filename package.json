{
  "name": "Kata github api autocomplete",
  "private": false,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages --dist dist --branch gh-pages --message \"Updated by gh-pages\""
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "sass-embedded": "^1.77.8",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "glob": "^11.0.0",
    "normalize.css": "^8.0.1"
  }
}
